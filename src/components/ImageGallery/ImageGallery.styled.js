
import styled from '@emotion/styled'

export const Pictures = styled.ul`
margin-top: 0;
margin-bottom: 0;
padding: 0;
margin-left: auto;
margin-right: auto;
display: grid;
max-width: calc(100vw - 48px);
grid-template-columns: repeat(auto-fill, minmax(320px, 1fr));
grid-gap: 10px;
list-style: none;

`;
